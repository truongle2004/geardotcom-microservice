services:
  geardotcom_postgres:
    image: postgres:15
    container_name: geardotcom_postgres
    environment:
      POSTGRES_DB: geardotcom
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: root
    volumes:
      - geardotcom_postgres_data:/var/lib/postgresql/data
    ports:
      - "5434:5432"
    networks:
      - ecommerce-network
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U postgres']
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    networks:
      - ecommerce-network
    healthcheck:
      test: ['CMD-SHELL', 'redis-cli ping | grep PONG']
      interval: 10s
      timeout: 5s
      retries: 5

  discovery-service:
    build: ./discovery-service
    container_name: discovery-service
    ports:
      - "8761:8761"
    networks:
      - ecommerce-network
    environment:
      - SPRING_PROFILES_ACTIVE=docker

  gateway-service:
    build: ./gateway
    container_name: gateway-service
    depends_on:
      - discovery-service
    environment:
      - SPRING_PROFILES_ACTIVE=docker
      - EUREKA_CLIENT_SERVICE_URL_DEFAULT_ZONE=http://discovery-service:8761/eureka
      - KEYCLOAK_REALM_URL=http://localhost:9000/realms/geardotcom       # ← public URL, matches token iss
      - KEYCLOAK_INTERNAL_URL=http://identity:9000/realms/geardotcom     # ← internal Docker DNS, for JWK fetch
    ports:
      - "8222:8222"
    networks:
      - ecommerce-network

  sale-service:
    build: ./sale-service
    container_name: sale-service
    depends_on:
      - geardotcom_postgres
      - discovery-service
      - gateway-service
      - redis
    volumes:
      - ./images:/app/uploads
    ports:
      - "8081:8081"
    environment:
      - SPRING_PROFILES_ACTIVE=docker
      - EUREKA_CLIENT_SERVICE_URL_DEFAULT_ZONE=http://discovery-service:8761/eureka
      - SPRING_DATASOURCE_URL=jdbc:postgresql://geardotcom_postgres:5432/geardotcom
      - SPRING_DATASOURCE_USERNAME=postgres
      - SPRING_DATASOURCE_PASSWORD=root
      - SPRING_IMAGE_DIR=/app/uploads
      - SPRING_UPLOAD_IMAGE_URL=http://discovery-service:8222/api/v1/sale/products
      - SPRING_REDIS_HOST=redis
      - SPRING_REDIS_PORT=6379
    networks:
      - ecommerce-network

  user-service:
    build: ./user-service
    container_name: user-service
    depends_on:
      - geardotcom_postgres
      - discovery-service
      - gateway-service
    ports:
      - "8083:8083"
    environment:
      - SPRING_PROFILES_ACTIVE=docker
      - EUREKA_CLIENT_SERVICE_URL_DEFAULT_ZONE=http://discovery-service:8761/eureka
      - SPRING_DATASOURCE_URL=jdbc:postgresql://geardotcom_postgres:5432/geardotcom
      - SPRING_DATASOURCE_USERNAME=postgres
      - SPRING_DATASOURCE_PASSWORD=root
    networks:
      - ecommerce-network

  payment-service:
    build: ./payment-service
    container_name: payment-service
    depends_on:
      - geardotcom_postgres
      - discovery-service
      - gateway-service
    ports:
      - "8082:8082"
    environment:
      - SPRING_PROFILES_ACTIVE=docker
      - EUREKA_CLIENT_SERVICE_URL_DEFAULT_ZONE=http://discovery-service:8761/eureka
      - SPRING_DATASOURCE_URL=jdbc:postgresql://geardotcom_postgres:5432/geardotcom
      - SPRING_DATASOURCE_USERNAME=postgres
      - SPRING_DATASOURCE_PASSWORD=root
      - VNP_TMN_CODE=test123
      - VNP_PAY_URL=http://localhost:9000/pay
      - VNP_SECRET_KEY=test_secret_key_123
      - VNP_API_URL=http://localhost:9000/api
      - VNP_RETURN_URL=http://localhost:8222/api/v1/sale/orders/return
    networks:
      - ecommerce-network

  # notification-service:
  #   build: ./notification-service
  #   container_name: notification-service
  #   depends_on:
  #     - geardotcom_postgres
  #     - discovery-service
  #     - gateway-service
  #   ports:
  #     - "8084:8084"
  #   environment:
  #     - SPRING_PROFILES_ACTIVE=docker
  #     - EUREKA_CLIENT_SERVICE_URL_DEFAULT_ZONE=http://discovery-service:8761/eureka
  #     - SPRING_DATASOURCE_URL=jdbc:postgresql://geardotcom_postgres:5432/geardotcom
  #     - SPRING_DATASOURCE_USERNAME=postgres
  #     - SPRING_DATASOURCE_PASSWORD=root
  #     - SPRING_KAFKA_BOOTSTRAP_SERVERS=kafka:9092
  #     - NOVU_API_KEY=test_novu_api_key
  #     - NOVU_BASE_URL=http://localhost:3000
  #   networks:
  #     - ecommerce-network

#  pgadmin:
#    image: dpage/pgadmin4:6.20
#    container_name: pgadmin
#    volumes:
#      - pgadmin:/var/lib/pgadmin
#    environment:
#      PGADMIN_DEFAULT_EMAIL: admin
#      PGADMIN_DEFAULT_PASSWORD: admin
#    networks:
#      - ecommerce-network

  keycloak_postgres:
    image: postgres:15
    container_name: keycloak_postgres
    environment:
      POSTGRES_DB: keycloak
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: root
    volumes:
      - keycloak_postgres_data:/var/lib/postgresql/data
    ports:
      - "5433:5432"
    networks:
      - ecommerce-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  identity:
    image: quay.io/keycloak/keycloak:21.0.2
    command: [ "start-dev --import-realm" ]
    container_name: keycloak
    environment:
      KEYCLOAK_ADMIN: admin
      KEYCLOAK_ADMIN_PASSWORD: admin
      KC_DB: postgres
      KC_DB_URL: jdbc:postgresql://keycloak_postgres:5432/keycloak
      KC_DB_USERNAME: postgres
      KC_DB_PASSWORD: root
      KC_HTTP_PORT: 9000
      KC_HOSTNAME_URL: http://localhost:9000         # ← stamps iss in token
      KC_HOSTNAME_ADMIN_URL: http://localhost:9000
      KC_HOSTNAME_STRICT: false
      KC_HOSTNAME_STRICT_HTTPS: false
    ports:
      - "9000:9000"
    volumes:
      - ./identity/themes/geardotcom/theme:/opt/keycloak/themes
      - ./identity/realm-export.json:/opt/keycloak/data/import/realm-export.json
    depends_on:
      - keycloak_postgres
    networks:
      - ecommerce-network

  # zookeeper:
  #   image: confluentinc/cp-zookeeper:7.5.0
  #   environment:
  #     ZOOKEEPER_CLIENT_PORT: 2181
  #     ZOOKEEPER_TICK_TIME: 2000
  #
  # kafka:
  #   image: confluentinc/cp-kafka:7.5.0
  #   depends_on:
  #     - zookeeper
  #   ports:
  #     - '9092:9092'
  #   environment:
  #     KAFKA_BROKER_ID: 1
  #     KAFKA_ZOOKEEPER_CONNECT: zookeeper:2181
  #     KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://localhost:9092
  #     KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
volumes:
  geardotcom_postgres_data:
  keycloak_postgres_data:
  redis_data:
#  pgadmin:

networks:
  ecommerce-network:
    driver: bridge
